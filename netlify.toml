[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA redirect — all paths serve index.html so React routing works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
